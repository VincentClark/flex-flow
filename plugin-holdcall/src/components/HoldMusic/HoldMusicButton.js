import React from 'react'
import { withTheme } from '@twilio/flex-ui';

import styled from 'react-emotion';
const HoldMusicButton = () => {
    return (
        <div>HoldMusicButton</div>
    )
}

export default withTheme(HoldMusicButton);