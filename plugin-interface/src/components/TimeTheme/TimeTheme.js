import React, { useState, useEffect } from 'react'
import { withTheme } from '@twilio/flex-ui';

const TimeTheme = () => {
    // const [message, setMessage] = useState(null);

    // const theDate = Date();
    // setNowTime(theDate.toString())

    return (
        <div>
            Now Time:
        </div>
    )
}

export default withTheme(TimeTheme)